{
  "name": "api",
  "private": true,
  "scripts": {
    "start": "meteor",
    "meteor:build": "meteor build . --architecture os.linux.x86_64",
    "meteor:build:install": "tar -xvf api.tar.gz && cd bundle/programs/server/ && npm install"
  },
  "dependencies": {
    "@babel/runtime": "^7.6.0",
    "bcrypt": "^3.0.7",
    "meteor-node-stubs": "^1.0.0"
  }
}
